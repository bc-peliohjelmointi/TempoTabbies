using UnityEngine;

public class korttiTekstinNimi : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
        CardManagerScript cardManager = GetComponent<CardManagerScript>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
